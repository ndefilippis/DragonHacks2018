// Take picture and return list of objects in the picture

function parseImageForObjects (img) {
  // TODO: Implement this
  // Also if we don't, just make sure this hard coded list matches our demo
  return {
    'apple': 4,
    'egg': 1,
    'bread': 1,
    'monster': 12
  }
}

module.exports = {parseImage: parseImageForObjects}
